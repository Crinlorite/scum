---
// Private live map (HTTP Basic Auth via nginx). Same island/POI map as the
// public /mapa but with the live player-position layer (polls /panel/live/positions.json).
import MapView from '../../components/MapView.astro';
---
<MapView lang="es" live noindex />
